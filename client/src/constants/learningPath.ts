export enum LearningPathsListAndRequestTypes {
  my = 'myList',
  shared = 'sharedList',
  public = 'publicList',
}

export enum LearningPathsRequestTypes {
  my = 'my',
  shared = 'shared',
  public = 'public',
}

export enum LearningPathsListTypes {
  myList = 'myList',
  sharedList = 'sharedList',
  publicList = 'publicList',
}
